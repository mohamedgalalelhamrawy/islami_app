

  import 'package:flutter/material.dart';

class Hadeth extends StatelessWidget {
    const Hadeth({super.key});
  
    @override
    Widget build(BuildContext context) {
      return Container(
      child: Center( 
        child: Text("Hadeth Tap",style: TextStyle(fontSize: 40),),
      ),
    );
    }
  }